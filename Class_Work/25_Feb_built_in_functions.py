# BUILT-IN FUNCTIONS
# Python provides many built-in functions like len(), type(), print(), sum()

numbers = [1,2,3,4,5]

print("Length:", len(numbers))
print("Sum:", sum(numbers))
print("Type:", type(numbers))
print("Max:", max(numbers))
print("Min:", min(numbers))